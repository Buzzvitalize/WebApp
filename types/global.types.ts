export type ProductionStat = {
  label: string;
  value: string;
  progress: number;
};

export type Mission = {
  title: string;
  description: string;
  reward: string;
};

export type RewardEntry = {
  label: string;
  amount: string;
};
